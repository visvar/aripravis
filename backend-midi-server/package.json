{
  "name": "backend-midi-server",
  "version": "1.0.0",
  "description": "Receives Web MIDI and sends it via WebSockets to the client.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.1.7",
    "webmidi": "^3.1.12"
  }
}
